# TrendPulse 舆情脉冲 — 演示视频脚本

预计时长：约 5 分钟

---

## 第一部分：项目介绍（约 40 秒）

### 画面：打开 README.md 或 PPT 展示

**旁白/字幕：**

> 大家好，这是我的考核项目 TrendPulse 舆情脉冲。
>
> 它是一个多源社交媒体舆情分析系统。输入一个关键词，系统会自动从 Reddit、YouTube、X(Twitter) 三个平台抓取数据，然后用 AI 生成情感分析、观点聚类和摘要报告。
>
> 比如我想知道大家怎么评价 "DeepSeek"，系统会告诉我：大家在夸它的代码能力，但吐槽推理速度慢，情感得分 85 分。

### 画面：展示 README 中的 Mermaid 架构图

**旁白/字幕：**

> 系统架构分为四层：
> - 前端用 Flutter Web 做可视化
> - 后端用 FastAPI 提供 RESTful API
> - 采集层支持三个平台：Reddit 用 Arctic Shift API，YouTube 用 yt-dlp，Twitter 用我自研的零成本采集方案
> - AI 层调用大模型做情感分析、观点聚类和摘要生成

---

## 第二部分：环境配置与启动（约 30 秒）

### 画面：终端 + .env 文件

**操作步骤：**

1. 打开 `backend/.env` 文件，展示配置项

**旁白/字幕：**

> 环境配置很简单，只需要填一个 LLM API Key。如果在国内访问 Twitter，还需要配一个代理地址。所有敏感信息都通过环境变量管理，不会提交到 Git。

2. 在终端运行 `./start.sh`

**旁白/字幕：**

> 一键启动脚本会自动启动后端 FastAPI 服务、构建 Flutter 前端、启动前端 HTTP 服务。可以看到后端在 8000 端口，前端在 3000 端口。

3. 快速展示 Swagger UI（http://localhost:8000/docs）

**旁白/字幕：**

> 后端提供完整的 Swagger API 文档，所有接口都可以在这里交互式测试。

---

## 第三部分：核心功能演示 — 数据采集（约 1 分 30 秒）

### 画面：打开前端页面 http://localhost:3000

### 3.1 创建采集任务

**操作步骤：**

1. 在搜索页面输入关键词，比如 "DeepSeek"
2. 选择数据源：勾选 Reddit、YouTube、Twitter 三个平台
3. 设置采集条数（比如 100 条/源）
4. 点击提交

**旁白/字幕：**

> 我输入关键词 "DeepSeek"，选择三个数据源，每个平台采集 100 条。点击提交后，系统会并发启动三个采集器。

### 3.2 展示采集过程（切到终端看日志）

**画面：终端日志滚动**

**旁白/字幕：**

> 看终端日志，三个平台在并发采集：
>
> Reddit 用的是 Arctic Shift 公开 API，不需要官方 API Key，直接批量搜索。可以看到它在按 Subreddit 搜索，每 500 条一批入库。
>
> YouTube 用的是 yt-dlp，也不需要 API Key。它会自动生成搜索变体来扩大覆盖面，每 200 条一批入库。
>
> Twitter 是最有意思的部分。我没有用官方 API（Basic 套餐要 100 美元/月），而是设计了一套零成本采集方案。可以看到日志里：
> - 先通过 DuckDuckGo 搜索 `site:x.com DeepSeek`，获取种子用户
> - 然后通过 Syndication API 获取用户时间线
> - 再批量获取推文详情，同时从 @mentions 中发现新用户
> - 这就是雪球式扩展——从 2 个种子用户，层层扩展到几十个用户，采集到上百条推文
>
> 整个过程完全免费，不需要任何 Twitter API Key。

### 3.3 采集完成

**旁白/字幕：**

> 三个平台都采集完成了。数据经过清洗管道处理——去重、过滤垃圾信息、去除乱码——然后入库。接下来系统会自动调用 AI 进行分析。

---

## 第四部分：核心功能演示 — AI 分析结果（约 1 分钟）

### 画面：切回前端页面

### 4.1 仪表盘页面

**操作步骤：**

1. 点击进入仪表盘页面

**旁白/字幕：**

> 这是仪表盘页面，展示 AI 分析的结果。
>
> 上面是舆情热度指标，根据采集数量和互动数（点赞、评论、转发）综合计算。
>
> 中间是情感分数仪表盘。可以看到颜色映射：绿色代表正面，黄色中性，红色负面。这个分数是 AI 对所有采集内容做情感分析后的综合得分。
>
> 下面是 3 个核心观点卡片。AI 从大量评论中提取了三个主要争议点，每个观点附带支持度百分比，加起来正好 100%。
>
> 最下面是 AI 生成的摘要，把几百条评论总结成一段 200-500 字的易读文本。

### 4.2 Token 成本控制（口头说明）

**旁白/字幕：**

> 这里说一下 Token 成本控制。几百条帖子直接发给 LLM 会很贵，所以我做了几个优化：
> - 用 tiktoken 精确计算 Token 数量
> - 超过 4000 Token 自动分段
> - 大批量文本用 Map-Reduce 模式：先分批分析，再合并结果
> - 还有关键句提取，减少不必要的 Token 消耗

---

## 第五部分：核心功能演示 — 源数据流（约 30 秒）

### 画面：切到源数据流页面

**操作步骤：**

1. 点击进入源数据流页面
2. 滚动浏览帖子列表
3. 点击一条帖子跳转原文

**旁白/字幕：**

> 这是源数据流页面，以列表形式展示所有抓取到的原始帖子。
>
> 每条帖子显示标题、来源平台图标、作者、时间和互动数据。支持分页浏览。
>
> 点击任意一条可以跳转到原文链接，比如这条 Reddit 帖子，点击后直接打开原始页面。

---

## 第六部分：加分项展示（约 1 分钟）

### 6.1 思维导图

**操作步骤：**

1. 切到思维导图页面

**旁白/字幕：**

> 这是加分项——思维导图。AI 把观点生成 Mermaid 格式的思维导图，前端渲染展示。可以直观看到关键词和各个观点的关系。

### 6.2 定时任务与报警

**操作步骤：**

1. 点击底部导航栏的「订阅」tab
2. 输入关键词（如 "DeepSeek"）
3. 选择采集间隔（1h / 6h / 12h / 24h）
4. 设置报警阈值（默认 30 分）
5. 点击「创建订阅」
6. 展示订阅列表，演示取消订阅

**旁白/字幕：**

> 另一个加分项是定时任务。点击底部的「订阅」tab，可以创建关键词订阅。
>
> 我输入 "DeepSeek"，选择每 6 小时采集一次，报警阈值设为 30 分——也就是说如果情感分数低于 30 分，系统会自动报警。
>
> 点击创建，可以看到订阅列表里出现了这条记录，显示关键词、采集间隔和报警阈值。
>
> 底层用的是 APScheduler 做定时调度，订阅信息持久化到 PostgreSQL，服务重启后自动恢复所有活跃订阅的定时任务。

### 6.3 反爬策略（口头说明）

**旁白/字幕：**

> 关于反爬策略，我在 TECHNICAL.md 里有详细说明：
> - User-Agent 轮换，5 种主流浏览器标识随机选择
> - 搜索引擎自动降级：DuckDuckGo 失败自动切 Google，再切 Bing
> - 请求间随机延迟，避免触发限流
> - 代理支持，国内环境通过代理访问
> - Twitter 的 Syndication API 是官方公开端点，本身就不需要认证，这是方案设计上的优势

---

## 第七部分：工程质量与总结（约 30 秒）

### 画面：快速切换展示代码和测试

**旁白/字幕：**

> 最后说一下工程质量：
>
> 代码方面，Python 全部有类型注解，关键函数都有中文 docstring，敏感信息通过环境变量管理。
>
> 测试方面，我用了 Hypothesis 属性测试框架，不只是测具体例子，而是验证通用的正确性属性。比如"任意合法推文 URL 都能正确解析 ID"、"输出中不存在重复数据"这类属性。单元测试加属性测试一共几十个。
>
> 总结一下，这个项目实现了：
> - 三个平台的数据采集（Reddit、YouTube、Twitter），其中 Twitter 用了自研的零成本方案
> - AI 情感分析、观点聚类、摘要生成，带 Token 成本控制
> - Flutter Web 前端可视化
> - 加分项：定时任务、思维导图、反爬策略文档
>
> 感谢观看。

---

## 录制建议

- 使用 OBS Studio 或 macOS 自带的屏幕录制（Cmd+Shift+5）
- 分辨率 1920x1080
- 建议用画外音讲解，语速适中
- 提前跑一次采集任务，确保演示时数据已就绪（AI 分析需要等几十秒）
- 可以提前准备两个关键词的结果：一个中文（如"DeepSeek"），一个英文（如"btc"）
- 输出格式：MP4
- 控制在 5 分钟左右，不要超过太多
